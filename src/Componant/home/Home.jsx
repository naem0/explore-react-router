import React from 'react';
import { Outlet } from 'react-router-dom';
import Nav from '../Nav/Nav';

const Home = () => {
    return (
        <div>
            <h2>this is Home</h2>
        </div>
    );
};

export default Home;